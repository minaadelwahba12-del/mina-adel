import React from 'react';
import ReactDOM from 'react-dom/client';
import ShopAccountsApp from './components/ShopAccountsApp';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ShopAccountsApp />
  </React.StrictMode>
);
